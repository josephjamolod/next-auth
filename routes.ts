export const publicRoutes = ["/", "/auth/verifyEmail"];

export const authRoutes = [
  "/auth/login",
  "/auth/register",
  "/auth/error",
  "/auth/resetPassword",
  "/auth/newPassword",
];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/settings";
